package com.example.inventory.user;

/**
 * ADMIN manages catalog data (products, categories, suppliers) and users.
 * STAFF records stock movements and reads everything.
 */
public enum Role {
    ADMIN,
    STAFF
}
